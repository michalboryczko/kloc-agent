---
milestone: v1.0
audited: 2026-05-16
status: tech_debt
scores:
  requirements: 26/26
  phases: 6/6
  integration: PASS
  flows: PASS
tech_debt:
  - id: TD-E2E-PRE-EXISTING
    source: Phase 3 SUMMARY + VERIFICATION
    detail: |
      7 pre-existing e2e test failures in tests/e2e/test_artifact_lifecycle.py
      and tests/e2e/test_hook_deny.py. Confirmed NOT regressions introduced
      by this milestone — they predate commit 13fd93f57 (the v1.0 baseline)
      and are unaffected by the Phase 1–6 changes. Root causes overlap with
      infra (TestClient + ASGI + S3 stub plumbing) rather than the AG-UI /
      runner correctness fixes shipped here. Defer to a future infrastructure
      / e2e-harness phase.
    severity: low
    action: defer
  - id: TD-IS-ALIVE-NO-TEST
    source: Phase 3 (ISS-10)
    detail: |
      `RegistryEntry.is_alive` TTL cache (~50 ms) ships without a dedicated
      cache-hit/miss regression test. Plan was a cleanup with documented
      "no new tests required" policy; surface stays small enough that
      regressions would be caught by integration tests, but a 5-line unit
      test would close the gap cheaply.
    severity: low
    action: opportunistic
  - id: TD-STREAMING-DOTS-ORPHAN
    source: Phase 5 (UI-P4, SC2 sub-item)
    detail: |
      `frontend/src/components/StreamingDots.tsx` and the `kloc-pulse-dot`
      keyframe in globals.css exist but are not wired to the chat input.
      Plan 05-02 made the documented decision to rely on CopilotKit's
      native streaming indicator; the StreamingDots primitive remains as
      an unused export. Either wire it in to satisfy the literal UI-P4
      spec ("3-dot pulse in chat input") OR delete the unused component.
      Human verification item routes this decision to the operator.
    severity: low
    action: operator_decision
  - id: TD-MIGRATIONS-COMMENTS
    source: Phase 3 (ISS-13)
    detail: |
      `migrations/versions/2026_05_14_0001_init.py:7-9` retains references
      to `research/04 §4.5` and `§1.2-1.5 in research/04`. Outside ROADMAP
      SC#5 grep scope (which targets `src/ runner/`) so does not block
      ISS-13, but inconsistent with the CLAUDE.md "stand alone without
      project context" policy. Single-file follow-up sweep.
    severity: trivial
    action: opportunistic
human_verification_aggregate:
  backend_boot:
    - "[Phase 2] Boot `uvicorn src.main:app` with LLM_PROVIDER=anthropic and no ANTHROPIC_API_KEY set; expect pydantic ValidationError naming ANTHROPIC_API_KEY (with KLOC_STUB_MODE remediation); MUST NOT reach the runner."
    - "[Phase 2] Boot the backend (or `make e2e-up`) without `/var/run/docker.sock` bind-mount (or with the Docker daemon stopped); expect lifespan to abort loudly with an aiodocker/DockerError traceback; uvicorn exits non-zero."
    - "[Phase 2] Boot `uvicorn src.main:app` with KLOC_HOOK_SECRET=dev-secret-please-rotate, ALLOW_HMAC_FALLBACK=true, KLOC_STUB_MODE unset; expect ValidationError naming both KLOC_HOOK_SECRET and KLOC_STUB_MODE remediations."
    - "[Phase 2] Run `make e2e-up` (or `docker compose up`) with Docker available; expect backend boots cleanly to `Application startup complete.`; session create + post message + SSE stream all work end-to-end after the lifespan branch collapse."
  backend_runtime_live_stack:
    - "[Phase 1] WR-02 persister idle-timeout budget under real LLM long-tail latency — confirm runner_heartbeat_timeout_s + 30s (default 60s) is wider than the longest normal between-event gap against real Gemini/Anthropic against the indexed PHP codebase."
    - "[Phase 1] Live runner spawn end-to-end with the audit drain on warm-idle eviction — under a real runner that emits N AfterToolCall events and is then evicted via warm-idle, post-shutdown `audit_log` row count for `tool_call.completed` matches N (no rows lost)."
  ui_visual:
    - "[Phase 4] Picker route loads with dark editorial-terminal background, amber radial gradients, subtle film-grain overlay, serif italic 'kloc agent.' display title with amber period, mono eyebrow labels in --text-dim."
    - "[Phase 4] Chat-view header is sticky 48px with backdrop-blur, serif italic 'kloc agent' wordmark with amber 'agent', mono [BETA] pill (not italic), truncated session id on right, '← sessions' ghost button works."
    - "[Phase 4] CopilotKit chat panel inherits the new theme — sidebar background = --bg-1 dark, accent buttons amber, mono separator color, no leftover blue from CopilotKit defaults."
    - "[Phase 4] Hover over a session row — row translates 3px right (160ms ease-out-snappy), arrow '→' fades in in --accent, bg shifts to --bg-1."
    - "[Phase 4] Open a ToolCallCard — 2px amber left edge, mono uppercase status pill, <details> chevron rotates 90° on open via group-open:rotate-90, <pre> blocks sit on darker --bg-0 well."
    - "[Phase 4] Denied tool-call (policy_violation) — card edge + pill switch to --danger red palette; card background --danger-soft."
    - "[Phase 5] On a 1440x900 viewport with a session active — chat panel fills the right column from header bottom to viewport bottom; SessionRail visible on the left (280px); no large dark void surrounding a floating sidebar (F-1 resolved)."
    - "[Phase 5] Reload the picker — headline fades up over 220ms; eyebrow / button / list arrive in a staggered 20-40-60ms cascade; reduced-motion users see no animation."
  ui_a11y:
    - "[Phase 5] Hover over an existing session row in the picker — row shifts ~3px right, arrow glyph fades in."
    - "[Phase 5] Observe RUNNER STATE pill in SessionRail while a runner is fresh/warm — status dot has a soft amber/green glow (box-shadow visible against dark background)."
    - "[Phase 5] Toggle ToolCallCard <details> arguments/result sections — chevron rotates 90 degrees with a 120ms ease on open; reverses on close."
    - "[Phase 5] Inspect compiled DOM of the chat textarea after mount — textarea has aria-label=\"Ask kloc analyst\"; persists across CopilotKit internal remounts (MutationObserver-applied)."
    - "[Phase 5] Send a message — confirm CopilotKit's native streaming indicator is visible and adequate for F-2 (or decide to wire StreamingDots — see TD-STREAMING-DOTS-ORPHAN)."
    - "[Phase 5] Run a manual WCAG AA contrast check on --text-mute and --text-dim against bg-0 / bg-1 — both meet WCAG AA (4.5:1 normal text or 3:1 large)."
  chat_smoke:
    - "[Phase 6] Picker → chat lazy-load smoke — on / (picker) DevTools Network shows no 1.5 MB CopilotKit chunk request; after clicking a session, the chunk loads and chat renders within ~1s without flicker or error overlay."
    - "[Phase 6] Chat hydration after dynamic import — header reads 'kloc agent BETA'; session subtitle reads 'session <8-char id>…'; textarea has focus; back button returns to picker preserving session list."
    - "[Phase 6] agent-proxy debug-flag — with NEXT_PUBLIC_DEBUG_HTTP=true set, server log emits `[agent-proxy] no session_id; keys=` with Object.keys names only; with the flag unset, no warn on the same malformed-shape input."
    - "[Phase 6] Error surface differentiation (D-3) — backend stopped → picker shows 'network unreachable — is the backend running?'; backend returning 500 → 'backend 500: <preview>'."
---

# v1.0 Hardening Milestone Audit

**Audited:** 2026-05-16
**Status:** tech_debt
**Result:** All 26 requirements satisfied; all 6 phases complete; cross-phase wiring verified end-to-end; 4 advisory tech-debt items + a consolidated 22-item human verification checklist routed to the operator.

## Verdict

The v1.0 hardening milestone is **code-complete**. All cross-phase contracts hold (Phase 2 settings → Phase 1/3 runtime consumers wired; Phase 5 deletions don't break Phase 6 references; Phase 4 token theme survives Phase 6's dynamic import). No BLOCKER findings. Status is `tech_debt` rather than `passed` because:

1. Four phases (2, 4, 5, 6) shipped with `human_needed` verifications that require live-stack / browser / WCAG-meter checks beyond what grep + unit tests can confirm.
2. Four advisory tech-debt items aggregated across the milestone (none regress the milestone goal; all are opportunistic).

## Phases

| # | Phase | Status | Plans | Tests | Notes |
|---|-------|--------|-------|-------|-------|
| 1 | Backend AG-UI & runner correctness | passed | 4/4 | 141 passed / 5 skipped / 0 failed | Two advisory live-stack human items (LLM tail-latency + audit-drain on eviction) |
| 2 | Backend settings & boot contract | human_needed | 3/3 | 166 passed / 5 skipped / 0 failed | Four production-shaped boot scenarios require live operator checks |
| 3 | Backend cleanup & comment sweep | passed | 5/5 | 178 in-scope passed (7 pre-existing e2e failures NOT regressions) | Phase 3 SC#5 gap (`dev-3` in `runner/__main__.py:209`) closed in commit 70a28c235 |
| 4 | UI foundations & component reskin | human_needed | 2/2 | npm build green; 136 backend unit tests preserved | Visual smoke of token theme, atmosphere, CopilotKit inheritance, hover/details rotation, denial branch |
| 5 | UI structural fix, polish, a11y, dead code | human_needed | 4/4 | npm build green; 0 inline styles; 0 dead-module imports | 8 visual/DOM-runtime/contrast checks |
| 6 | Frontend code quality | human_needed | 4/4 | tsc clean; eslint clean; 25/25 frontend tests pass; 136/137 backend unit tests pass | 4 runtime-only checks (lazy-load, hydration, debug-flag log, error string) |

## Requirements Coverage (26 / 26)

3-source cross-reference:
- **REQUIREMENTS.md traceability table** (lines 110–137) maps all 26 REQ-IDs to phases.
- **Phase VERIFICATION.md Requirements Coverage tables** confirm each REQ-ID has implementing code + tests.
- **Plan SUMMARY frontmatter** (`requirements:` field, where present) declares the REQ each plan owns.

| Requirement | Phase | Plan | Verification Status | Cross-Source Match |
|-------------|-------|------|---------------------|--------------------|
| ISS-01 | 1 | 01-01 | SATISFIED — `src/api/internal.py:160` publish-then-flush + regression test | OK |
| ISS-02 | 1 | 01-02 | SATISFIED — `src/api/stream.py:120-140` persist_tasks dict; unit + integration tests | OK |
| ISS-03 | 1 | 01-03 | SATISFIED — `runner/hooks/audit.py:70-103` cancel→drain→aclose; 5 tests | OK |
| ISS-04 | 1 | 01-01 | SATISFIED — `src/api/internal.py:176` CAS guard; 3 tests | OK |
| ISS-06 | 1 | 01-04 | SATISFIED — `runner/channel.py` full-attempt transcript replay (CR-02 strengthened); 5 tests | OK |
| ISS-05 | 2 | 02-01 | SATISFIED — `src/api/stream.py:412-413` reads settings.llm_provider / settings.llm_model_id only; no `os.environ` | OK |
| ISS-07 | 2 | 02-02 | SATISFIED — `src/settings.py:167-177` 3-AND HMAC fallback predicate + 8-cell truth table | OK |
| ISS-12 | 2 | 02-03 | SATISFIED — `src/main.py:97-111` unconditional DockerRunner; no `kloc_runner_mode` field | OK |
| ISS-08 | 3 | 03-01 | SATISFIED — `Settings.diag_events` consumed in `src/api/internal.py:42` and `src/api/webhooks.py:53` | OK |
| ISS-09 | 3 | 03-02 | SATISFIED — `src/main.py:75,80` plain assignments; no PEP-526 annotations | OK |
| ISS-10 | 3 | 03-03 | SATISFIED — `RegistryEntry.is_alive` 50 ms TTL cache; both call sites updated | OK |
| ISS-11 | 3 | 03-04 | SATISFIED — 499 / 204 branches at `src/api/internal.py:267-279`; 2 regression tests | OK |
| ISS-13 | 3 | 03-05 | SATISFIED — Sweep complete; SC#5 grep returns 0 matches (gap closed in 70a28c235); CLAUDE.md policy rewritten | OK |
| UI-P0 | 4 | 04-CONTEXT | SATISFIED — 4 decisions materialized in code (Tailwind v4, shadcn, dark-only, next/font) | OK |
| UI-P1 | 4 | 04-01 | SATISFIED — globals.css token theme + @theme block; 3 fonts; atmosphere; 6 CopilotKit vars overridden | OK |
| UI-P2 | 4 | 04-02 | SATISFIED — ToolCallCard, AgentBody, SessionPicker (extracted), page.tsx header reskinned; 0 inline styles | OK |
| UI-P3 | 5 | 05-01 | SATISFIED — `<CopilotSidebar>` removed; inline `<CopilotChat>` + SessionRail mounted | OK |
| UI-P4 | 5 | 05-02 | SATISFIED with documented deviation — 5/6 sub-items VERIFIED; streaming indicator deferred to CopilotKit native (operator decision) | OK |
| UI-P5 | 5 | 05-03 | SATISFIED — A11yChatTextarea (A-1), --text-mute/dim tokens (A-2), aria-label (A-3), aria-busy (A-4), h1/h2 hierarchy (A-5) | OK |
| UI-P6 | 5 | 05-04 | SATISFIED — All 4 dead modules deleted; 0 lingering imports | OK |
| FE-PERF | 6 | 06-01 | SATISFIED — RenderToolCall module-scope, INITIAL_AGENT_STATE hoisted, per-row formatting memoized | OK |
| FE-BUNDLE | 6 | 06-01 | SATISFIED — CopilotRuntime at module scope; CopilotKit dynamic-imported (97.5% bundle reduction on `/`) | OK |
| FE-ROUTES | 6 | 06-02 | SATISFIED — runtime/dynamic/fetchCache exports both routes; 30s timeout via AbortSignal.any | OK |
| FE-DATA | 6 | 06-03 | SATISFIED — AbortSignal threaded; useEffect cleanup; pickCtrlRef dedup; ApiError/NetworkError differentiation | OK |
| FE-SEC | 6 | 06-02 | SATISFIED — debug-gated body dump; CSRF posture in `frontend/docs/csrf.md`; validator + 20-assertion regression test | OK |
| FE-QUALITY | 6 | 06-04 | SATISFIED — BusyState union, IncomingMessage tightened, BROWSER_BACKEND_URL centralized, eslint flat-native, tsc + eslint clean | OK |

**Orphans:** 0 — every REQ-ID in REQUIREMENTS.md maps to a phase, every phase claims its expected REQ-IDs, every plan SUMMARY's `requirements:` matches the verification table.

## Cross-Phase Integration

### Wiring Summary

- **Connected:** 8 expected cross-phase contracts, all verified end-to-end
- **Orphaned exports:** 1 (`StreamingDots.tsx` — documented deviation, routed to tech-debt)
- **Missing connections:** 0
- **Broken flows:** 0

### Backend ↔ Backend

| Contract | From | To | Status | Evidence |
|----------|------|----|--------|----------|
| `Settings.diag_events` field → `_diag` gates | Phase 3 SUMMARY 03-01 (added field) | Phase 3 internal.py + webhooks.py | WIRED | `src/settings.py:132` declares field; `src/api/internal.py:42` and `src/api/webhooks.py:53` both call `get_settings().diag_events` |
| `settings.llm_provider` / `settings.llm_model_id` resolution | Phase 2 SUMMARY 02-01 | Phase 1's modified `src/api/stream.py` (post-ISS-02 dedup) | WIRED | `src/api/stream.py:412-413` reads both off settings; no `os.environ` reads remain |
| `DockerRunner` unconditional construction | Phase 2 SUMMARY 02-03 | Phase 1's runner spawn paths | WIRED | `src/main.py:97-111` constructs single DockerRunner; ImportError aborts boot (test_lifespan_boot.py pins) |
| `persist_tasks[(session_id, run_id)]` dedup + CAS pop on `active_by_session` | Phase 1 SUMMARY 01-02 + 01-01 | Phase 3's `_diag` gating in same files | WIRED | Phase 3 cleanup did not touch the dedup or CAS code paths; greps confirm both still in place at `stream.py:133` and `internal.py:176` |
| `yielded_this_attempt` runner transcript | Phase 1 SUMMARY 01-04 | Backend internal.py ingress (orphan flush + CAS) | WIRED | `runner/channel.py` prepends transcript on both 4xx and exception branches; receiving side at `src/api/internal.py` ordering invariant + CAS deal with the over-delivery correctly |

### Backend ↔ Frontend

| Contract | From | To | Status | Evidence |
|----------|------|----|--------|----------|
| Phase 6 `agent-proxy` `AbortSignal.any([req.signal, AbortSignal.timeout(30_000)])` | Phase 6 SUMMARY 06-02 | Backend SSE lifecycle (Phase 1 ISS-02 dedup + CR-01 reconnect race fix) | WIRED | `agent-proxy/route.ts:94-110` propagates abort to upstream backend; backend's persister-dedup ensures concurrent reconnect POSTs (which the client may issue on abort + retry) don't double-spawn; CR-01 register-before-replay in `stream_get` closes the GET reconnect race |
| Phase 6 `validation.ts` body schema | Phase 6 SUMMARY 06-02 | Phase 1's `_dispatch_frame` ingress | WIRED | Validator returns 400 before any upstream call; backend ingress only sees well-formed shapes |

### Phase 5 → Phase 6 (Deletion Continuity)

| Contract | Status | Evidence |
|----------|--------|----------|
| Phase 5 deletes `ChatWindow.tsx`, `Composer.tsx`, `agui-http-agent.ts`, `sseParser.ts`; Phase 6 must not re-reference any | WIRED | `grep -rnE "ChatWindow\|Composer\|agui-http-agent\|sseParser" frontend/src/` returns empty post-Phase-6; `ls` confirms all four files absent |

### Phase 4 → Phase 6 (Theme Survival)

| Contract | Status | Evidence |
|----------|--------|----------|
| Phase 4 globals.css token theme survives Phase 6 dynamic-import refactor | WIRED | `frontend/src/app/layout.tsx:3` still imports `./globals.css`; postcss.config.mjs still mounts `@tailwindcss/postcss`; ChatShell.tsx imports `@copilotkit/react-ui/styles.css` co-located with the lazy chunk, so override cascade (globals AFTER copilotkit) still wins in both server-rendered shell and dynamic chunk |
| Phase 4 SessionPicker + Phase 5 SessionRail/A11yChatTextarea/AgentBody continue to mount under Phase 6 layout | WIRED | `page.tsx` imports SessionPicker at line 14 + ChatShell dynamic at lines 18-21; `ChatShell.tsx` imports SessionRail, A11yChatTextarea, AgentBody and mounts them inside `<CopilotKit>` (lines 7-9, 52, 54, 56) |

## E2E Flows

| Flow | Steps | Status |
|------|-------|--------|
| Session create | Picker → `listSessions({ signal })` → backend `POST /v1/sessions` → SessionPicker renders rows → pick → SessionRail card | COMPLETE — Phase 4 reskin + Phase 6 AbortController dedup confirmed |
| Spawn runner | Pick session → backend lifespan-bound `DockerRunner` → `RunnerRegistry.get_or_spawn` (50ms is_alive cache) → container starts | COMPLETE — Phase 2 unconditional construction + Phase 3 cache wired |
| Stream chat | `CopilotChat` (dynamic-imported) → `agent-proxy/route.ts` (validated body + 30s timeout) → backend `POST /v1/sessions/{id}/stream` → `persist_tasks[(sid,rid)]` (no double-spawn) → AG-UI events forwarded → CopilotChat renders | COMPLETE — Phase 1 dedup + Phase 6 validation + Phase 5 inline CopilotChat all wired |
| Tool call | Runner emits BeforeToolCall webhook → HMAC verify (3-AND fallback validator gates rotated secret) → policy check → AfterToolCall webhook → audit_log row | COMPLETE — Phase 2 HMAC fallback validator + Phase 1 audit drain on shutdown |
| Reconnect | Mid-stream transport reset → runner `channel.py` `yielded_this_attempt` prepended on retry → backend `_dispatch_frame` publishes RUN_STARTED before orphan flush → `active_by_session` CAS guard on terminal frames → stream_get register-before-replay (CR-01) | COMPLETE — Phase 1 transcript replay + ordering invariant + CAS + CR-01 seam |
| Audit row written | AfterToolCall webhook → `AuditHookSender.stop()` cancel-then-drain queue on warm-idle eviction → POST every queued payload → audit_log rows match completed-tool-call count | COMPLETE — Phase 1 ISS-03 drain (with WR-05 hardening: cancel before drain). Live-stack confirmation routed to human verification. |

## Tech Debt

See frontmatter `tech_debt:` block. Four items, all advisory:

1. **TD-E2E-PRE-EXISTING** — 7 pre-existing e2e failures in `test_hook_deny.py` + `test_artifact_lifecycle.py`. Pre-date `13fd93f57` baseline; explicitly NOT regressions. Defer to future infra phase.
2. **TD-IS-ALIVE-NO-TEST** — `RegistryEntry.is_alive` 50 ms cache has no dedicated unit test. Cleanup phase policy permitted no new tests; opportunistic follow-up.
3. **TD-STREAMING-DOTS-ORPHAN** — `StreamingDots.tsx` + `kloc-pulse-dot` keyframe exist but not wired; spec said "3-dot pulse in chat input" but plan deferred to CopilotKit native indicator. Operator decision: wire it OR delete.
4. **TD-MIGRATIONS-COMMENTS** — `migrations/versions/2026_05_14_0001_init.py:7-9` retains `research/04 §...` refs. Outside ISS-13 SC#5 scope; trivial follow-up.

## Human Verification Checklist

See frontmatter `human_verification_aggregate:`. 22 items grouped by domain:

- **Backend boot (4)** — Phase 2 production-shaped boot scenarios: missing provider key, missing Docker daemon, default HMAC secret with fallback, end-to-end happy path
- **Backend runtime / live stack (2)** — Phase 1 LLM tail-latency budget + warm-idle eviction audit drain row-count
- **UI visual (8)** — Phase 4 (6 visual items) + Phase 5 (2 visual items): atmosphere, header, CopilotKit inheritance, hover, ToolCallCard interactions, denial branch, F-1 viewport fix, picker entrance animation
- **UI a11y / micro-interactions (6)** — Phase 5: hover-shift, runner-state glow, chevron rotation, MutationObserver aria-label, streaming indicator decision, WCAG AA contrast measurement
- **Chat smoke (4)** — Phase 6: picker→chat lazy-load Network audit, chat hydration sequence, debug-flag log gating, error string differentiation

## Conclusion

The milestone delivers exactly what `PROJECT.md` defined as "demo-stable for internal beta use." Every backend correctness invariant has implementing code AND a regression test that would have caught the original bug. Every UI design decision is materialized in code (not just documented). Every frontend code-quality finding from the three review directories is closed with verifiable evidence.

The integration audit confirms the 22 plans across 6 phases compose into a working product: Phase 2's settings flow into Phase 1's runtime; Phase 3's cleanup doesn't undo Phase 1's correctness; Phase 5's deletions don't break Phase 6's references; Phase 6's dynamic-import doesn't lose Phase 4's token theme. The E2E path session-create → spawn → stream → tool-call → reconnect → audit-row is wired end-to-end at the code level; only operator confirmation against a live stack remains.

Status `tech_debt` is the honest read: code is complete, all 26 requirements satisfied, but the milestone ships with 22 human-verification items (consolidated above) and 4 advisory tech-debt notes (none blocking). Once the operator clears the live-stack checklist, status promotes to `passed`.

---

*Audit produced by milestone integration audit subagent.*
*Verification methodology: 3-source cross-reference (REQUIREMENTS.md ↔ phase VERIFICATION.md ↔ plan SUMMARY frontmatter) plus direct codebase greps for each cross-phase contract.*
