---
phase: 05
plan: 02
subsystem: frontend-ui
type: feat
autonomous: true
wave: 2
depends_on: ["05-01"]
requirements: [UI-P4]
tags: [ui, motion, polish, animations]
---

# Phase 5 Plan 02: Polish — animations, glows, metadata

## Objective

Add CSS-only animations and small typographic polish that lifts the redesigned
UI from "good" to "memorable":

- Picker entrance fade-up + 20ms cascade
- Session-row hover translate + arrow reveal (mostly in place; verify glow)
- Runner-state pill active glow
- Streaming-indicator 3-dot pulse keyframe
- Tool-card chevron rotation on `<details[open]>` (already partly in place; verify)
- `<title>` + `<meta>` polish in layout.tsx

## Context

- `frontend/src/app/globals.css` — `@layer utilities` for animation classes.
- `frontend/src/components/SessionPicker.tsx` — entrance animation targets.
- `frontend/src/components/SessionRail.tsx` — pill glow already present from 05-01.
- `frontend/src/components/ToolCallCard.tsx` — chevron rotation via `group-open:rotate-90`.
- `frontend/src/app/layout.tsx` — metadata title + description.

## Tasks

<task id="1" type="auto">
  <name>Add @keyframes + animation utilities in globals.css</name>
  <files>frontend/src/app/globals.css</files>
  <behavior>
    Append `@layer utilities` block with:
    - `@keyframes kloc-fade-up { from { opacity:0; transform: translateY(8px); } to { opacity:1; transform: none; } }`
    - `.animate-kloc-fade-up` applying it at 220ms with snappy ease.
    - `.animate-kloc-fade-up-delay-1` (20ms), `-delay-2` (40ms), `-delay-3` (60ms).
    - `@keyframes kloc-pulse-dot` for streaming indicator (40% peak).
    - `.animate-kloc-pulse-dot` and `.kloc-pulse-dot-2` / `-3` with 0.15s/0.30s delays.
  </behavior>
  <done>Classes exist in compiled CSS; no errors.</done>
</task>

<task id="2" type="auto">
  <name>Apply entrance animation classes in SessionPicker</name>
  <files>frontend/src/components/SessionPicker.tsx</files>
  <behavior>
    Apply `.animate-kloc-fade-up` to the headline; `-delay-1` to eyebrow, `-delay-2`
    to button row, `-delay-3` to the list. Use Tailwind's `motion-reduce:` prefix
    to skip animations under `prefers-reduced-motion`.
  </behavior>
  <done>Picker animates on first paint; reduced-motion users see no animation.</done>
</task>

<task id="3" type="auto">
  <name>Add streaming-indicator dots component + mount in chat pane</name>
  <files>frontend/src/components/StreamingDots.tsx, frontend/src/app/page.tsx</files>
  <behavior>
    Create `StreamingDots` (small 3-dot component). Use `<CopilotChat onInProgress>`
    callback to drive an isInProgress flag in page state; render dots inside the
    chat header strip when streaming.

    Actually simpler: rely on CopilotChat's own loading. Skip the wiring if no clear
    integration point — leave the keyframe class available in globals for future use.
    Document the decision in the SUMMARY.
  </behavior>
  <done>Either component rendered + wired, or decision documented to defer to CopilotKit-native indicator.</done>
</task>

<task id="4" type="auto">
  <name>Polish layout.tsx metadata</name>
  <files>frontend/src/app/layout.tsx</files>
  <behavior>
    Update `metadata.title` to "kloc agent" (no hyphen), set `description` to
    "Analyst chat over an indexed codebase. Live agent reasoning, MCP tool
    calls, persistent audit log." Add `metadataBase` + `openGraph`/`twitter`
    minimal entries if cheap.
  </behavior>
  <done>Title and description updated; build clean.</done>
</task>

<task id="5" type="auto">
  <name>Verify chevron rotation on ToolCallCard</name>
  <files>frontend/src/components/ToolCallCard.tsx</files>
  <behavior>
    Read the existing classes — `group-open:rotate-90` should already be on
    the `▸` span. If missing, add it. (Likely no-op.)
  </behavior>
  <done>Chevron rotates 90° when `<details>` opens.</done>
</task>

## Verification

- `cd frontend && npm run build` succeeds.
- Visual: picker fade-up on reload; rail pill glow visible.
- `grep '@keyframes kloc-' frontend/src/app/globals.css` returns 2+ matches.

## Success Criteria

- [ ] 5+ animation classes / keyframes defined in globals.css
- [ ] Picker uses entrance animation
- [ ] Metadata title + description updated
