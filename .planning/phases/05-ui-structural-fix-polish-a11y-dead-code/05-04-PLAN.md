---
phase: 05
plan: 04
subsystem: frontend-ui
type: chore
autonomous: true
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
requirements: [UI-P6]
tags: [ui, dead-code, cleanup]
---

# Phase 5 Plan 04: Dead code deletion (UI-P6)

## Objective

Delete the four files self-documented as unused now that UI-P1..UI-P5 have
landed:

- `frontend/src/components/ChatWindow.tsx` — alternate inline-chat surface, never wired (page.tsx now uses `<CopilotChat>` directly).
- `frontend/src/components/Composer.tsx` — bare textarea primitive, never imported.
- `frontend/src/lib/agui-http-agent.ts` — direct AG-UI HttpAgent helper, the CopilotKit path doesn't use it.
- `frontend/src/utils/sseParser.ts` — fallback SSE parser, supplanted by `@ag-ui/client`.

Re-evaluate `AgentBody.tsx`: per UI-P3, artifacts moved to SessionRail. The
file now only registers the wildcard tool-call renderer. Decision: keep
(single-responsibility, ~25 lines), or inline into page.tsx. Decide based
on whether keeping the file improves separation of concerns.

## Tasks

<task id="1" type="auto">
  <name>Delete ChatWindow, Composer, agui-http-agent, sseParser</name>
  <files>
    frontend/src/components/ChatWindow.tsx,
    frontend/src/components/Composer.tsx,
    frontend/src/lib/agui-http-agent.ts,
    frontend/src/utils/sseParser.ts
  </files>
  <behavior>
    Use `git rm`. Run grep to confirm no imports remain.
  </behavior>
  <done>Files removed; grep returns nothing for the four module names.</done>
</task>

<task id="2" type="auto">
  <name>AgentBody decision</name>
  <files>frontend/src/components/AgentBody.tsx (optional)</files>
  <behavior>
    Decide: keep as-is (clean 30-line single-purpose file) or inline its
    `useCopilotAction({ name: "*" })` hook directly into page.tsx. Document
    the call in the SUMMARY.
  </behavior>
  <done>Decision documented.</done>
</task>

<task id="3" type="auto">
  <name>Final build + backend test verification</name>
  <files>(none)</files>
  <behavior>
    Run `cd frontend && npm run build` for clean compile.
    Run `uv run python -m pytest tests/unit/ -q` for backend regression.
  </behavior>
  <done>Both green.</done>
</task>

## Verification

- `grep -rn "ChatWindow\|Composer\|agui-http-agent\|sseParser" frontend/src/` empty.
- `npm run build` clean.
- Backend unit tests pass.
