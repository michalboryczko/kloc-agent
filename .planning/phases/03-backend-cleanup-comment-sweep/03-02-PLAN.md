---
phase: 03
plan: 02
subsystem: backend
type: auto
autonomous: true
wave: 2
requirements: [ISS-09]
files_modified:
  - src/main.py
task_count: 1
---

# Plan 03-02: ISS-09 — Delete unused annotated assignments

## Objective

Remove the two annotated assignments on `app.state.*` in `src/main.py`. They
look like type hints but Python evaluates them as expressions: noise without
a real contract. Replace each with a plain assignment.

## Tasks

<task id="1" type="auto">
  <behavior>app.state.active_run_by_session / pending_pre_run_started still initialised as empty dicts; no behaviour change.</behavior>
  <implementation>
    - Rewrite `app.state.active_run_by_session: dict[str, str] = {}` to
      `app.state.active_run_by_session = {}`.
    - Same for `pending_pre_run_started`.
  </implementation>
  <files>src/main.py</files>
  <verification>uv run --frozen pytest tests/unit/test_internal.py -q</verification>
  <done>Both annotated assignments removed; tests still pass.</done>
</task>
