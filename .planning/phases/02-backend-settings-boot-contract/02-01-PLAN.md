---
phase: 02-backend-settings-boot-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings.py
  - src/api/stream.py
  - tests/unit/test_settings.py
autonomous: true
requirements:
  - ISS-05
must_haves:
  truths:
    - "`src/api/stream.py` no longer reads `LLM_PROVIDER` or `LLM_MODEL_ID` from `os.environ`; both values come from `Settings`."
    - "`Settings` exposes an `llm_model_id` field that resolves to the provider-appropriate default when unset."
    - "Booting with a configured provider but missing API key raises a `ValidationError` at `Settings()` construction (preserved from existing validator)."
    - "Hydration payloads built by `_build_hydration` use `settings.llm_provider` / `settings.llm_model_id` verbatim."
  artifacts:
    - path: "src/settings.py"
      provides: "llm_model_id field; provider-key validator unchanged in semantics"
      contains: "llm_model_id"
    - path: "src/api/stream.py"
      provides: "Hydration build with no os.environ reads for provider/model"
      contains: "settings.llm_model_id"
    - path: "tests/unit/test_settings.py"
      provides: "Regression tests for ISS-05"
      contains: "llm_model_id"
  key_links:
    - from: "src/api/stream.py"
      to: "src/settings.py"
      via: "get_settings().llm_provider / .llm_model_id"
      pattern: "settings\\.llm_(provider|model_id)"
---

<objective>
ISS-05: route `llm_provider` and `llm_model_id` through `Settings` only. Remove the
`os.environ.get("LLM_PROVIDER")` and `os.environ.get("LLM_MODEL_ID")` reads in
`src/api/stream.py` so misconfiguration surfaces at boot (the existing
`_validate_provider_key` validator), not inside the runner.

Purpose: per D-01..D-04, `Settings` is the single source of truth for provider
config. Per-session model selection is explicitly deferred (deferred ideas in
02-CONTEXT.md).

Output:
- `Settings.llm_model_id` field with provider-aware default resolution.
- `src/api/stream.py` reads provider+model from `settings` only.
- Regression tests in `tests/unit/test_settings.py` that would have caught the
  silent-env-override behaviour.
</objective>

<execution_context>
@/Users/michal/dev/ai/kloc/kloc-agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michal/dev/ai/kloc/kloc-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-settings-boot-contract/02-CONTEXT.md
@src/settings.py
@src/api/stream.py
@tests/unit/test_settings.py

<interfaces>
<!-- Key contracts the executor needs. Do NOT re-derive from the codebase. -->

From src/settings.py (existing):
```python
LlmProvider = Literal["anthropic", "openrouter", "bedrock", "gemini"]

class Settings(BaseSettings):
    # ... existing fields ...
    llm_provider: LlmProvider = "gemini"
    anthropic_api_key: str | None = None
    gemini_api_key: str | None = None
    stub_mode: bool = Field(default=False, validation_alias="KLOC_STUB_MODE", ...)

    @model_validator(mode="after")
    def _validate_provider_key(self) -> "Settings":
        if self.stub_mode: return self
        if self.llm_provider == "anthropic" and not self.anthropic_api_key:
            raise ValueError("ANTHROPIC_API_KEY required ...")
        if self.llm_provider == "gemini" and not self.gemini_api_key:
            raise ValueError("GEMINI_API_KEY required ...")
        return self
```

Current `src/api/stream.py:427-438` (the lines to delete):
```python
llm_provider = os.environ.get("LLM_PROVIDER") or settings.llm_provider
model_id_default = (
    "gemini-3.1-pro-preview"
    if llm_provider == "gemini"
    else "claude-3-5-haiku-20241022"
)
model_id = os.environ.get("LLM_MODEL_ID", model_id_default)
```

After this plan, the block becomes a direct read of `settings.llm_provider` and
`settings.llm_model_id`.

`HydrationPayload` fields used here: `llm_provider`, `model_id` (see
`src/db/models.py` — already exists; not modified).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `llm_model_id` to Settings with provider-aware default</name>
  <files>src/settings.py, tests/unit/test_settings.py</files>
  <behavior>
    - When `LLM_MODEL_ID` env var is unset and `llm_provider="gemini"`, `Settings().llm_model_id == "gemini-3.1-pro-preview"`.
    - When `LLM_MODEL_ID` env var is unset and `llm_provider="anthropic"`, `Settings().llm_model_id == "claude-3-5-haiku-20241022"`.
    - When `LLM_MODEL_ID` env var is set, `Settings().llm_model_id` reflects the env value verbatim regardless of provider.
    - Existing `_validate_provider_key` behaviour preserved: missing key raises when not `stub_mode`; passes when `stub_mode=True`.
    - `llm_model_id` survives the model_validator pass (no spurious mutation).
  </behavior>
  <action>
    Per D-01 and D-04, add a new `llm_model_id: str | None = Field(default=None, ...)` field to `Settings` and resolve the provider-aware default inside an existing or new `model_validator(mode="after")` body. Keep the resolution logic identical to the current `src/api/stream.py:433-438` block (`"gemini-3.1-pro-preview"` for gemini, `"claude-3-5-haiku-20241022"` otherwise) so behaviour is unchanged.

    Wire the resolution at the bottom of `_validate_provider_key` (or a sibling validator at Claude's discretion per CONTEXT D-Discretion) so that after construction, `settings.llm_model_id` is always a non-empty `str` (never `None`).

    Add three regression tests to `tests/unit/test_settings.py`:
    1. `test_llm_model_id_defaults_to_gemini_model_when_provider_gemini` — set `KLOC_STUB_MODE=true`, `LLM_PROVIDER=gemini`, delenv `LLM_MODEL_ID`, assert `s.llm_model_id == "gemini-3.1-pro-preview"`.
    2. `test_llm_model_id_defaults_to_anthropic_model_when_provider_anthropic` — set `KLOC_STUB_MODE=true`, `LLM_PROVIDER=anthropic`, delenv `LLM_MODEL_ID`, assert `s.llm_model_id == "claude-3-5-haiku-20241022"`.
    3. `test_llm_model_id_env_override_wins` — set `LLM_MODEL_ID=custom-model-x`, assert `s.llm_model_id == "custom-model-x"` regardless of provider.

    Do NOT change the existing `_validate_provider_key` semantics — the existing four `test_*_key_*` tests must keep passing.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_settings.py -x -q</automated>
  </verify>
  <done>
    - `grep -n "llm_model_id" src/settings.py` shows the field declaration and (if separate) the resolution validator.
    - All settings tests pass; new tests assert the three default/override branches.
    - `grep -c "llm_model_id" tests/unit/test_settings.py` returns >= 3.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove `os.environ.get` reads from `src/api/stream.py` and use Settings</name>
  <files>src/api/stream.py, tests/unit/test_settings.py</files>
  <behavior>
    - After this task, `grep -n "os.environ.get" src/api/stream.py` returns no matches for `LLM_PROVIDER` or `LLM_MODEL_ID`.
    - `_build_hydration` (current name at `src/api/stream.py:~390-455`) constructs `HydrationPayload` with `llm_provider=settings.llm_provider` and `model_id=settings.llm_model_id` verbatim.
    - If `os` is no longer used anywhere else in the file, the `import os` is removed; otherwise it stays.
    - A unit-style import test (no DB, no I/O) constructs `Settings(_env_file=None, stub_mode=True, llm_provider="anthropic", anthropic_api_key="sk-test")` and asserts `s.llm_model_id == "claude-3-5-haiku-20241022"` — confirming the stream.py call site can rely on the settings field exclusively.
  </behavior>
  <action>
    Per D-01, replace the block currently at `src/api/stream.py:427-438` with direct settings reads:
    - `llm_provider = settings.llm_provider`
    - `model_id = settings.llm_model_id`

    Delete the now-dead `model_id_default` ternary entirely; the default lives on the Settings validator from Task 1.

    Scan the rest of `src/api/stream.py` for any remaining `os.environ` reads. If none exist after the change, remove `import os` from line 15. If other `os.*` usage remains, keep the import.

    In `tests/unit/test_settings.py`, add `test_stream_call_site_uses_settings_only` which:
    1. Sets `KLOC_STUB_MODE=true`, `LLM_PROVIDER=anthropic`, `ANTHROPIC_API_KEY=sk-test`, delenvs `LLM_MODEL_ID`.
    2. Constructs `Settings(_env_file=None)`.
    3. Asserts `s.llm_provider == "anthropic"` and `s.llm_model_id == "claude-3-5-haiku-20241022"`.
    4. Adds a comment that this test pins the contract `src/api/stream.py:_build_hydration` relies on after ISS-05.

    Note: this is a unit-level regression test for ISS-05 per CLAUDE.md test policy. We do NOT add a full FastAPI integration test for `_build_hydration` — the existing settings cover is sufficient because the stream.py change is mechanical substitution.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_settings.py tests/unit/test_stream.py -x -q && grep -cE "os\\.environ\\.get.*(LLM_PROVIDER|LLM_MODEL_ID)" src/api/stream.py | grep -q '^0$'</automated>
  </verify>
  <done>
    - `grep -nE "os\\.environ\\.get.*(LLM_PROVIDER|LLM_MODEL_ID)" src/api/stream.py` returns nothing.
    - `grep -n "settings.llm_model_id" src/api/stream.py` returns the new assignment line.
    - `grep -n "settings.llm_provider" src/api/stream.py` returns the new assignment line.
    - `tests/unit/test_settings.py` includes `test_stream_call_site_uses_settings_only`.
    - `tests/unit/test_stream.py` (if it covers `_build_hydration`) still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env -> backend process | Operator-controlled env vars flow into `Settings` at boot. |
| backend process -> runner container | `HydrationPayload` (which carries `llm_provider` and `model_id`) is written to a hydration file the runner reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `Settings.llm_model_id` resolution validator | mitigate | Resolve default inside `model_validator(mode="after")` so any future code path that re-validates Settings (e.g. test overrides) gets the same provider-aware default. No external input is trusted for the default value. |
| T-02-02 | Information Disclosure | `os.environ.get("LLM_PROVIDER")` runtime override | mitigate | Removed entirely; operator can no longer flip the provider per-request via process env without a Settings re-construct + boot, eliminating a silent provider mismatch path that could route requests to the wrong key. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies introduced in this plan. RESEARCH.md Package Legitimacy Audit not required. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_settings.py -x -q` — all settings tests pass including 4 new regression tests.
- `uv run pytest tests/unit/test_stream.py -x -q` — existing stream tests stay green.
- `grep -cE "os\\.environ\\.get.*(LLM_PROVIDER|LLM_MODEL_ID)" src/api/stream.py` returns `0`.
- `grep -n "llm_model_id" src/settings.py` shows the field + resolution.
- Manual: starting the backend with `LLM_PROVIDER=anthropic` and no `ANTHROPIC_API_KEY` aborts with `ValidationError` (the existing validator — verifies the call-site removal did not regress boot-time validation).
</verification>

<success_criteria>
- ISS-05 ROADMAP success criterion 1 satisfied: `llm_provider`/`llm_model_id` flow through `Settings`; raw `os.environ.get` reads removed from `src/api/stream.py`; missing key still raises at boot (existing validator preserved).
- All locked decisions D-01, D-02, D-03, D-04 implemented as specified.
- Regression tests would catch a future regression where a contributor reintroduces `os.environ.get("LLM_PROVIDER")` reads.
</success_criteria>

<output>
Create `.planning/phases/02-backend-settings-boot-contract/02-01-SUMMARY.md` when done.
</output>
