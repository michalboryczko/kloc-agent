require("reflect-metadata");
const require_runtime = require('../../../_virtual/_rolldown/runtime.cjs');
const require_package$1 = require('../../../package.cjs');

//#region src/v2/runtime/telemetry/scarf-client.ts
var import_package = /* @__PURE__ */ require_runtime.__toESM(require_package$1.default);
const SCARF_BASE_URL = `https://copilotkit.gateway.scarf.sh/${import_package.version}`;
var ScarfClient = class {
	constructor() {}
	async logEvent(properties) {
		try {
			const controller = new AbortController();
			const timeoutId = setTimeout(() => controller.abort(), 3e3);
			const queryParams = new URLSearchParams();
			Object.entries(properties).forEach(([key, value]) => {
				if (value !== null && value !== void 0) queryParams.append(key, String(value));
			});
			const url = `${SCARF_BASE_URL}?${queryParams.toString()}`;
			const response = await fetch(url, {
				method: "GET",
				signal: controller.signal
			});
			clearTimeout(timeoutId);
			if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);
		} catch {}
	}
};
var scarf_client_default = new ScarfClient();

//#endregion
exports.default = scarf_client_default;
//# sourceMappingURL=scarf-client.cjs.map